# AllSpeak training curriculum — draft for review

> **Working document.** Goal: a self-contained curriculum that lets AI agents and human coders write idiomatic AllSpeak.
>
> Background: original idioms outline in `260429-00.md`, additions in `260429-01.md`, review and merge notes in `260512-00.md`.
>
> This file lays out the proposed shape (`learn/reference/` + `learn/idioms/`), drafts the full tables of contents, and writes out **one reference file** and **one idiom file** in full so the format can be felt before more is built. Everything below is a draft for you to redline.

---

## 1. Proposed shape

Two tiers under a single `learn/` root.

```
learn/
  README.md
  reference/    # canonical language reference
  idioms/       # patterns and worked examples
```

**Reference** answers *what is this thing in AllSpeak?* — symbols, variables, collections, control flow, modules. Stable, canonical, encyclopedic. Re-read when memory fades.

**Idioms** answers *how do I do X the AllSpeak way?* — short pattern catalogs with worked examples, anti-patterns, and pointers back into reference. Read once per pattern, then occasionally for refresh.

Each file targets 200–600 words for an idiom; reference files can run longer (500–1500) where the concept warrants. One concept per file. Anti-patterns made explicit. Cross-references at the bottom of every file.

Format: Markdown. Headings + fenced code blocks. Language-neutral examples in English — language-specific syntax belongs in the language packs, not the curriculum.

---

## 2. Reference — table of contents

| # | File | Covers |
|---|------|--------|
| 01 | `structure.md` | Domains, the compiler-tries-each-domain model, how plugins extend vocabulary. |
| 02 | `symbols-and-layout.md` | The 4 symbols (`!`, backtick, `:`, `-`), doc-block markers (`!!` / `!!!`), indenting, variable naming, blank-line conventions. |
| 03 | `variables-and-arrays.md` | All variables are arrays, single-element default, `index` cursor model, scratch variables, the `variable` type vs typed variables. *(drafted below)* |
| 04 | `collections.md` | Arrays vs dictionaries vs lists; object properties; JS/Python divergence; when to pick which shape. |
| 05 | `values-and-types.md` | Numbers, strings, booleans; the typed-but-flexible model; explicit conversion. |
| 06 | `conditions.md` | What a condition is; equality, comparison, presence; domain-specific conditions. |
| 07 | `arithmetic.md` | Integer-first arithmetic; `add/take/multiply/divide … giving`; `modulo`; floats-as-backtick-strings and the scaled-integer pattern. |
| 08 | `strings-and-text.md` | Backtick literals, multi-line strings, `cat`, common string operations (`replace`, `from`/`to`, length-of, position-of). |
| 09 | `control-flow.md` | `if`/`else`, `begin`/`end`, `while`, `for each`, labels, `gosub`/`return`, `stop`/`exit`. |
| 10 | `errors-and-recovery.md` | `or` and `on failure`; single-statement vs block form; what counts as a failure. |
| 11 | `cooperative-multitasking.md` | `fork`, `wait`, `stop`; the never-interrupted-mid-command guarantee; how threads yield. |
| 12 | `modules.md` | Module variables; loading and running; import/export; `release parent`; messaging; when to extract one. |
| 13 | `plugins.md` | What a plugin is; the contract (link to `spec/`); when to write one vs use a module. |
| 14 | `browser-and-webson.md` | DOM variable types; layout-in-JSON vs logic-in-AS; `attach`; creating DOM-element arrays. |
| 15 | `multilingual.md` | The `language` directive; language packs; writing logic that survives translation. |
| 16 | `doc-blocks.md` | The `!!` / `!!!` convention; what goes in the prose; `asdoc-check`; the review-while-documenting workflow. |

---

## 3. Idioms — table of contents

| # | File | Pattern |
|---|------|---------|
| 01 | `cat-and-string-building.md` | `cat` goes **between** values, never before the first; template-style construction; multi-line backtick strings. |
| 02 | `event-handlers-and-array-index.md` | `on click X gosub H` for an array X; how H knows which element fired (`the index of`). *(drafted below)* |
| 03 | `looping-patterns.md` | `while`, `for each`, label loops — which to pick when. |
| 04 | `picking-a-collection-shape.md` | Variable-array vs dict-of-dicts vs list-of-dicts; ergonomic trade-offs; access-pattern criteria. |
| 05 | `floats-and-scaled-integers.md` | Floats arrive as backtick strings; scale to integers for arithmetic; rescale for display. |
| 06 | `rest-and-async.md` | `rest get`/`rest post`; error blocks; server-side iteration vs script-side. |
| 07 | `mqtt-pubsub.md` | Topic conventions; dict-shaped payloads; the chat-server pattern. |
| 08 | `webson-and-as-separation.md` | Layout in `.json`, logic in `.as`; the `attach` binding; create-then-index for arrays of elements. |
| 09 | `extracting-a-module.md` | When a section is module-shaped; export-at-launch vs message-on-demand; pointer to `as-modularize` skill. |
| 10 | `writing-language-neutral.md` | Choosing structure that survives translation; avoiding English-only assumptions. |
| 11 | `debugging-as.md` | `print`, `log`, the trace flags; reading the runtime view; reproducing in `conformance/`. |
| 12 | `working-with-ai.md` | AI writes / human reviews; what "readable" demands; spotting hallucinations; when to ask the AI to explain rather than rewrite. |

---

## 4. Sample — Reference 03

> *This is the sample reference file. Tone, depth, and structure are the things to react to.*

# Variables and arrays

Variables in AllSpeak are containers. A variable can hold a value (number, string, boolean) or represent an entity (a DOM element, a file, a module). Each domain — Core, Browser, REST, MQTT, plugin domains — defines its own variable types with their own vocabulary.

## Naming and scope

- Variable names start with a capital letter. Camel case is fine — `Counter`, `UserName`, `MessageList`.
- All variables are global. AllSpeak has no block scope and no function-local variables. The only way to get private state is to run a child module (see [modules](modules.md)).
- Name variables for what they hold, not how they're used. A button representing the primary action is `PrimaryButton`, not `Btn1`.
- Group variables by type and by function, not alphabetically.
- Scratch variables — short-lived reusables like `I`, `N`, `Temp` — are best grouped together and separated from main variables by a blank line.

## All variables are arrays

Every variable is an array. By default it has one element, so most of the time you can ignore the array nature entirely:

```as
variable Counter
put 0 into Counter        ! Counter[0] = 0
add 1 to Counter          ! Counter[0] = 1
```

When you need more than one slot, grow the array with `set the elements of`:

```as
set the elements of Counter to 5    ! Counter now has 5 slots, [0]..[4]
```

Growing preserves existing contents; shrinking loses the high-indexed values.

## The cursor model

Access to a specific element is via an internal pointer set with `index`:

```as
index Counter to 2
put 42 into Counter       ! writes to Counter[2]
```

Once indexed, the variable behaves as if it has a single element. There is **no other syntax for indexed access** — no `Counter[2]` notation, no `element 2 of Counter`. The cursor is the only way in, similar to SQL cursors. This is intentional: most code can ignore that arrays exist, and code that needs them is forced to be explicit about which element it's touching.

## Mixed types within an array

Elements of an array are independent. A single variable can hold a number in one slot and a string in another — though doing so usually signals a missed modelling opportunity (see [picking-a-collection-shape](../idioms/picking-a-collection-shape.md)).

## When to reach for arrays

The clearest signal is **several variables doing much the same thing**. Three buttons named `SaveButton`, `LoadButton`, `QuitButton` that all share handlers and styling want to be one 3-element `Button` array. This applies to DOM elements as much as to scalar data — arrays of `div`, `input`, `button` are routine in any non-trivial UI.

If you find yourself naming variables `Item1`, `Item2`, `Item3`: stop, use an array.

## The `variable` type

`variable` is the one weakly-typed shape: it can hold numeric, string, or boolean values, with mostly-automatic conversion. Other types — `file`, `button`, `dictionary`, module-handles — are strict about what they accept.

## JS vs Python

Both implementations follow the same model for scalar variables and arrays. They diverge on collections: Python exposes `dictionary` and `list` as distinct typed shapes; JS unifies storage as strings and converts to objects on the way in and out. See [collections](collections.md) for the implications.

## Related

- [collections](collections.md) — when an array element should itself be a dictionary or list.
- [picking-a-collection-shape](../idioms/picking-a-collection-shape.md) — choosing between array, dict, and list.
- [event-handlers-and-array-index](../idioms/event-handlers-and-array-index.md) — how event handlers find out which array element fired.

---

## 5. Sample — Idiom 02

> *This is the sample idiom file. Shorter, more pattern-focused, with explicit anti-pattern.*

# Event handlers and array index

## Problem

You have an array of UI elements — say five buttons — and you want a single handler that knows which one was clicked.

## Pattern

Attach the same handler to every element. Inside the handler, read `the index of` the array to find out which slot fired.

```as
button Button
set the elements of Button to 5
! ... create the buttons, give them captions ...

set N to 0
while N is less than 5
    on click Button [N] gosub HandleClick
    add 1 to N

stop

HandleClick:
    set Which to the index of Button
    print `Button ` cat Which cat ` was clicked`
    return
```

The runtime sets the cursor on `Button` to the firing element's index *before* entering the handler. `the index of Button` inside `HandleClick` is the correct slot.

## Why this works

`gosub HandleClick` is a single statement; the handler is just a labelled subroutine, not a closure capturing `N` at registration time. The runtime gives the handler access to the firing element by setting the cursor — the same cursor model used everywhere else (see [variables-and-arrays](../reference/variables-and-arrays.md)).

## Anti-pattern: writing five handlers

```as
on click Button [0] gosub HandleClick0
on click Button [1] gosub HandleClick1
...
```

This works but defeats the array. Five near-identical subroutines that differ only in a constant should be one subroutine that reads `the index of`.

## Anti-pattern: capturing the loop variable

```as
while N is less than 5
    on click Button [N] gosub HandleClick
    add 1 to N

HandleClick:
    print `Button ` cat N cat ` was clicked`   ! WRONG — N is whatever the loop left it
    return
```

There is no closure. `N` at handler-time is whatever the most recent code wrote to it — usually 5, not the firing index. Always read `the index of` inside the handler.

## Multi-line handlers

For more than a single statement, use `begin … end`:

```as
on click Button gosub HandleClick

HandleClick:
    set Which to the index of Button
    if Which is 0 begin
        ! special case for the first button
        gosub HandleSpecial
        return
    end
    print `Generic handler for ` cat Which
    return
```

Note: `gosub` not `on click … begin … end`. Event-handler statements are single statements; the multi-line shape lives inside the subroutine.

## Related

- [variables-and-arrays](../reference/variables-and-arrays.md) — the cursor model this idiom relies on.
- [control-flow](../reference/control-flow.md) — `gosub` and `return`.
- [webson-and-as-separation](webson-and-as-separation.md) — how Button arrays usually get created from layout.

---

## 6. Open questions

Things to decide before drafting the rest:

1. **Location.** `learn/` at repo root, or under `docs/`? Existing `/codex/` is tutorial-IDE; `/spec/` is contracts. `learn/` reads cleanly as a third sibling.
2. **Audience tone.** Drafts above assume a reader who knows another programming language and is new to AllSpeak. Should there be a separate ground-floor track for first-time programmers, or is `/codex/` already that?
3. **Language of examples.** I've used English. Once a reference file stabilises, do we mirror it into FR/IT/DE, or leave the curriculum English-only and trust the language packs to handle translation at the script level?
4. **Cross-references.** The drafts use file-relative links. Are those workable in your reading flow (editor + GitHub web view), or would you prefer something else?
5. **Numbering.** The TOCs use numeric prefixes. Pros: ordering is explicit. Cons: inserting between 03 and 04 means renumbering. Alternative: no prefixes, ordering in `README.md` only.
6. **Anti-patterns.** I've leaned heavily into them in the idiom sample. Worth it for AI training? Or noise?
7. **Code-example verification.** Both samples include code I believe is correct but haven't run. Worth wiring drafts into `conformance/` so examples are guaranteed-runnable?

## 7. What I need from you

Mark up this file directly. Three useful kinds of feedback:

- **TOC entries**: add, remove, rename, reorder.
- **Sample files**: edits to tone, depth, structure of the two drafted sections. These set the template for everything that follows, so getting them right is the highest-leverage feedback now.
- **Open questions**: any you can settle.

When the shape is agreed, I'll draft the remaining files one at a time, starting with whichever you nominate.
